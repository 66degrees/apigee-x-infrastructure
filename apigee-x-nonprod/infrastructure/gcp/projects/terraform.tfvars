projects = {
  "nonprod-project" = {
    name            = ""
    project_id      = "non-prod apigee"       # Must be globally unique
    folder_id       = ""       # If the folder is pre-existing, otherwise update manually
    org_id          = ""                # Your GCP organization ID
    billing_account = ""     # Your billing account ID
  }
}